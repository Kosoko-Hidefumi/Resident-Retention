export type TermSummaryRow = {
  期: string;
  総数: string;
  沖縄出身_県内勤務: string;
  沖縄出身_県外勤務: string;
  沖縄出身_勤務先不明: string;
  県外出身_県内勤務: string;
  県外出身_県外勤務: string;
  県外出身_勤務先不明: string;
  出身不明_県内勤務: string;
  出身不明_県外勤務: string;
  出身不明_勤務先不明: string;
};

export type UniversityRow = {
  期: string;
  総数: string;
  琉球大学出身_県内_人数: string;
  その他大学出身_県外_人数: string;
  琉球大学出身_県内_pct: string;
  その他大学出身_県外_pct: string;
};

export const WORK_CATEGORY_KEYS = [
  { key: "沖縄出身_県内勤務", label: "沖縄出身→県内", short: "沖→県内" },
  { key: "沖縄出身_県外勤務", label: "沖縄出身→県外", short: "沖→県外" },
  { key: "沖縄出身_勤務先不明", label: "沖縄出身→不明", short: "沖→?" },
  { key: "県外出身_県内勤務", label: "県外出身→県内", short: "外→県内" },
  { key: "県外出身_県外勤務", label: "県外出身→県外", short: "外→県外" },
  { key: "県外出身_勤務先不明", label: "県外出身→不明", short: "外→?" },
  { key: "出身不明_県内勤務", label: "出身不明→県内", short: "?→県内" },
  { key: "出身不明_県外勤務", label: "出身不明→県外", short: "?→県外" },
  { key: "出身不明_勤務先不明", label: "出身不明→不明", short: "?→?" },
] as const;

export const CATEGORY_COLORS = [
  "#22d3ee",
  "#f97316",
  "#64748b",
  "#34d399",
  "#a78bfa",
  "#94a3b8",
  "#fbbf24",
  "#fb7185",
  "#e879f9",
];
